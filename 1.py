class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        res = [-1, -1]

        for i in range(len(nums)):
            comp = target-nums[i]
            if comp in hash_map:
                return [i, hash_map.get(comp)]
            hash_map[nums[i]] = iclass Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        res = [-1, -1]

        for i in range(len(nums)):
            comp = target-nums[i]
            if comp in hash_map:
                return [i, hash_map.get(comp)]
            hash_map[nums[i]] = iclass Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        res = [-1, -1]

        for i in range(len(nums)):
            comp = target-nums[i]
            if comp in hash_map:
                return [i, hash_map.get(comp)]
            hash_map[nums[i]] = iclass Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        res = [-1, -1]

        for i in range(len(nums)):
            comp = target-nums[i]
            if comp in hash_map:
                return [i, hash_map.get(comp)]
            hash_map[nums[i]] = iclass Solution:
            
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        res = [-1, -1]

        for i in range(len(nums)):
            comp = target-nums[i]
            if comp in hash_map:
                return [i, hash_map.get(comp)]
            hash_map[nums[i]] = i